/*
Q$("#greeting-form") .on("submit", function(event_details) {
var greeting = "Hi ";
var name = this.name.value;
    var greetingmessage = greeting + name;
    $("#greeting-form").hide(800,function() {
        $("#greeting").append("<p>" + greetingmessage + "</p>");
    });
    event_details.preventDefault();
})

$("#email-form") .on("submit", function(event_details) {
    var greetings = "Thank you! ";
    var name = this.email.value;
    var greetingemail = greetings + name;
    $("#email-form").hide(800,function() {
        $("#greeting").append("<p>" + greetingemail + "</p>");
    });
    event_details.preventDefault();
})
 */
